import {ActionQuiz, IQuizState} from "./types";


export const quizReducer = (state: IQuizState, action: ActionQuiz) => {
    switch (action.type) {

        default:
            return state
    }

}
